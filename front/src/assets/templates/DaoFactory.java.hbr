package {{pj.id}};

import java.sql.*;
import com.entity.*;
import java.util.*;
import com.dao.impl.*;

//数据工厂
public class DAOFactory{
    {{#each pj.entities}}
        //获取{{this.name}}的数据访问对象
        public static {{this.id}}DAO get{{this.id}}DAOInstance(){
            return new {{this.id}}DAOImpl();
        }
    {{/each}}

}